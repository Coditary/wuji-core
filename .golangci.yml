run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused

issues:
  exclude-dirs:
    - api/proto/v1
  exclude-rules:
    - path: pkg/driver/image_proto\.go
      linters:
        - staticcheck
      text: "SA1019:"
